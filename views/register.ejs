<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>register</title>
    <style>
        * {
            padding: 0;
            margin: 0;
            overflow: hidden;
            font-family: 'Times New Roman', Times, serif
        }

        .wrapper {
            height: auto;
            width: 100vw;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        form {
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            gap: .8rem;
            margin-top: 15rem;
        }

        input {
            border-left: none;
            border-top: none;
            border-right: none;
            outline: none;
            height: 2rem;
            width: 15rem;
            padding: .5rem;
            /* border-radius: .2rem; */
            /* background-color: #b1b1b1; */
            color: #2d56e8;
            letter-spacing: .01rem;
            font-size: .9rem;
        }

        .button {
            background-color: #2d56e8;
            color: #fff;
            width: 9rem;
            border: none;
            cursor: pointer;
            margin-top: 1rem;
        }

        .button:hover {
            background-color: #4568e6;

        }

        h2 {
            position: absolute;
            top: 1rem;
            left: 1rem;
            text-transform: uppercase;
        }

        /* For WebKit browsers (Chrome, Safari) */
        input[type=number]::-webkit-inner-spin-button,
        input[type=number]::-webkit-outer-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }
    </style>
</head>

<body>
    <div class="wrapper">
        <h2>REGISTER</h2>
        <form action="/user/register" method="post">
            <input type="email" name="email" id="" placeholder="email" autocomplete="off">
            <input type="password" name="password" id="" placeholder="password">
            <input type="text" name="username" autocomplete="off" id="" placeholder="username">
            <input type="number" name="age" id="" placeholder="age">
            <input type="submit" value="crate new user" class="button">
        </form>
    </div>
</body>

</html>